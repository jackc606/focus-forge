"""MCP server that lets an AI agent drive a running Focus Forge editor.

Run with ``python -m focusforge_mcp`` (cwd = repo root). It proxies tool calls over a
loopback socket to the editor's AI Bridge (enable it in the toolbar). See ``server.py``.
"""
